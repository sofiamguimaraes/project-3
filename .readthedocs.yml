version: 2

sphinx:
  configuration: _config.yml  # Matches the location of your Jupyter Book config

conda:
  file: environment.yml

build:
  os: ubuntu-22.04  # Ensure compatibility
  tools:
    python: "3.9"
